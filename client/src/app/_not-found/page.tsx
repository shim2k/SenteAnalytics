import { Metadata } from "next";
import { redirect } from "next/navigation";

export const metadata: Metadata = {
  title:
    "Sente Analytics",
  description: "Connect with your customers and make better decisions with Sente Analytics.",
};

export default function Home() {
  return (
    <div>asdf</div>
  );
}
